<div class="row section-body playlist-tiles">
  {{#each model as |playlist|}}
    {{mf-playlist-tile playlist=playlist}}
  {{else}}
    <h3 class="text-center">No playlists found</h3>
  {{/each}}
</div>