<div class="row">
  <ul>
    {{#each model as |track|}}
      {{mf-track track=track}}
    {{/each}}
  </ul>
</div>

<div class="row">
  <button type="button" class="button" {{action 'play'}}>Play</button>
  <button type="button" class="button" {{action 'stop'}}>Stop</button>
</div>