<div class="section-body">
  {{#each model as |tltrack|}}
    {{mf-track track=tltrack.track tlid=tltrack.tlid isQueue=true}}
  {{else}}
    <h3 class="text-center">Queue is empty</h3>
  {{/each}}
</div>